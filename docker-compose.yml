version: '3.8'

services:
  trace-app:
    build: .
    container_name: trace-app
    ports:
      - "3001:3001"
    volumes:
      - trace-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  trace-data:
